{
  "name": "tic-tac-toe",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "client": "yarn --cwd client dev",
    "server": "yarn --cwd server start",
    "dev": "npm-run-all --parallel client server"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
